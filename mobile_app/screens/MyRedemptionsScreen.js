import React, { useCallback, useState } from 'react';
import { ActivityIndicator, FlatList, Text, TouchableOpacity, View } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';
import { ArrowLeft } from 'lucide-react-native';
import { useFocusEffect, useNavigation } from '@react-navigation/native';
import { api } from '../api/axios';

export default function MyRedemptionsScreen() { const navigation = useNavigation(); const [rows,setRows]=useState([]); const [loading,setLoading]=useState(true); const [error,setError]=useState(''); const load=useCallback(async()=>{setLoading(true);setError('');try{setRows((await api.get('/impacto/canjes')).data||[]);}catch(e){setError(e.userMessage);}finally{setLoading(false);}},[]); useFocusEffect(useCallback(()=>{load();},[load])); return <SafeAreaView className="flex-1 bg-gray-50"><View className="flex-row items-center bg-white px-5 py-4"><TouchableOpacity onPress={()=>navigation.goBack()} className="h-10 w-10 items-center justify-center rounded-full bg-gray-100"><ArrowLeft color="#111827" size={20}/></TouchableOpacity><Text className="ml-4 text-xl font-black">Mis canjes</Text></View>{loading?<ActivityIndicator className="mt-8" color="#047857"/>:null}{error?<TouchableOpacity onPress={load} className="m-5 rounded-2xl bg-red-50 p-4"><Text className="text-center font-bold text-red-700">{error}{'\n'}Reintentar</Text></TouchableOpacity>:null}<FlatList data={rows} keyExtractor={x=>String(x.id)} contentContainerStyle={{padding:20,gap:12}} ListEmptyComponent={!loading&&!error?<Text className="text-center text-gray-500">Aún no tienes canjes.</Text>:null} renderItem={({item})=><View className="rounded-2xl bg-white p-5"><Text className="text-lg font-black">{item.recompensa}</Text><Text className="mt-2 font-bold text-emerald-700">{item.puntos_utilizados} puntos</Text><Text className="mt-1 text-gray-500">Estado: {item.estado}</Text></View>}/></SafeAreaView>; }
